# RAWviewer 發布說明

**Language / 語言：** [English](RELEASE_NOTES.md) · 繁體中文


## 🚀 版本 3.1.0
**狀態：尚未正式發布 — 目前位於 `development` 分支**

本版的重點是局部遮罩：獨立分頁、全新引擎，以及配套的調整項目。此外還有影像合成（HDR、全景、景深合成）、一般影像檔的非破壞性編輯、相機色彩校正，以及大量效能改善。

### 🎭 局部遮罩

- **遮罩擁有獨立分頁**，並依「各控制項作用的對象」分區：遮罩堆疊、建立遮罩的方式、對選取遮罩的操作、筆刷，以及該遮罩自己的調整項目。
- **五種建立遮罩的方式**，全部可見、各一鍵：**Brush**、**AI Selection**、**Smart Object**、**Sky**、**Depth**——依實際排列順序列出，AI Selection 排在最前面，因為它最快且不需下載專屬模型。除 Brush 外都會自行建立遮罩，因此選了工具又改變主意不會留下空遮罩。
- **遮罩可以群組。** 將一個遮罩拖曳到另一個上即可合併為單一遮罩（共用一組調整值）；把其中一部分拖出即可還原。兩種操作皆可復原。
- **每個遮罩都有自己的調整項目**，分組方式與 Global 分頁一致——**Light**（曝光、對比）、**Color**（色溫、色調、飽和度、鮮豔度）與 **Detail**（銳利化、清晰度、去朦朧、去色邊）。各組可收合，點擊數值即可歸零，與 Global 分頁相同。
- **Add / Erase / Invert / Duplicate / Delete** 作用於選取的遮罩。Duplicate 會連同覆蓋範圍與調整值複製為另一個遮罩以便重新定位；Delete 刪除的是遮罩，而不是相片。
- **遮罩列會顯示遮罩本身**，而不只是名稱：每一列都會描繪該遮罩的形狀，反轉後的遮罩也會顯示（並標示）它實際覆蓋的區域。
- **一次只顯示一個遮罩疊色。** 同時出現兩塊色彩無法分辨，因此顯示某個遮罩時會隱藏其他遮罩。編輯覆蓋範圍（Add／Erase）時會顯示正在修改的遮罩；移動調整滑桿時則隱藏疊色，讓你判斷對相片的實際效果。<kbd>M</kbd> 可隨時覆寫。
- **Invert 作用於整個畫面**，而不只是你塗抹過的部分——反轉後的遮罩會作用在原本未覆蓋的所有區域。

### 🤖 AI 遮罩 *(Plus)*

- **Smart Object** 與 **AI Selection** 以「由誰決定」命名——由 App 或由你。**Sky** 一鍵遮罩天空。
- **AI Selection 是點選工具，不是筆刷**：每次點擊都是一個點，多次點擊會加入同一個遮罩。它排在 AI 工具的最前面——速度約為 Smart Object 的十倍，且不需下載專屬模型。選取範圍內部實心、邊緣銳利。
- **Smart Object 耗時最久，但邊緣最乾淨。** 一鍵、無須瞄準，即使畫面中突出的是多個分開的物體，也只回傳單一遮罩。當去背需要承受強烈調整時選它；想指定特定物體、或想立刻得到結果時選 AI Selection。
- **Depth** 依距離而非依主體建立遮罩：最近的物體為全強度，愈遠愈淡。可用來提亮背景、為遠景加上空氣感，或讓風景的遠半部偏冷。它是漸層而非去背——整張照片都被遮罩，只是程度不同——因此要調整背景時將它反轉即可。一般照片約需一秒。
- **等待時會顯示進度對話框**，可取消並具逾時機制。首次使用需下載模型，該階段不套用逾時。
- **若相片已有相同遮罩，會選取既有的那一個**而非重複堆疊；已產生遮罩的工具會停用並說明原因。
- **若模型找不到目標，就不會新增遮罩**並明確告知，而不是留下一個空的「Sky」遮罩。**AI Selection** 點到空白時會忘記該次點擊並告知，不會留下毫無覆蓋的遮罩；它只拒絕真正的空選取，不會因為選取範圍小而拒絕，因此仍可挑出單一朵花。**Depth** 不受此限制：它是整張畫面的漸層，因此「大部分是遠景、只有一個近景主體」是好的深度遮罩，而非空遮罩。
- **Standard 版完全不提供這些項目**——不會出現無法使用的灰色按鈕，遮罩分頁也不會觸發任何模型下載。

### 🖌️ 筆刷

- **在 Global 分頁，按住 <kbd>D</kbd> / <kbd>B</kbd> / <kbd>X</kbd> / <kbd>H</kbd> 並移動游標即可塗抹**，不需按住滑鼠鍵。放開按鍵即結束筆畫並收起工具；若偏好持續啟用，面板上的按鈕仍可鎖定工具。
- **在遮罩分頁，<kbd>P</kbd> 與 <kbd>X</kbd> 切換 Add／Erase**——按一次武裝、再按一次收起；武裝期間掃過或拖曳即可塗抹。**Brush** 只建立空遮罩、不會自動武裝 Add；準備塗抹時再按 <kbd>P</kbd>（或 Add 按鈕）。
- **新增 Brush Feather**，與 Size、Flow、Edge Assist 一併可在遮罩分頁直接調整，不需離開該頁。先前筆刷邊緣固定為最柔和，這正是塗滿區域需要反覆塗抹的原因。
- **橡皮擦真的能擦除。** 先前它受 Edge Assist 限制，越過邊緣的塗抹無法清除；更糟的是放開筆刷後會還原約三分之一剛擦掉的內容。
- **雙指捲動可在筆畫進行中調整筆刷大小**，水平捲動調整 Flow，且不會中斷筆畫。
- **筆畫結束時不再於遮罩上留下方形邊緣。**
- **空的筆刷遮罩在復原後仍會保留。** 建立筆刷遮罩會立刻寫入，因此第一次塗抹後按 Ctrl+Z 會還原筆畫，而不是清掉整個遮罩堆疊。

### 🌄 影像合成

- **HDR 合成、全景拼接與 HDR 全景拼接。** 在圖庫多選後點擊右鍵。支援手持拍攝 MTB 曝光自動對齊、亮部／暗部／中間調權重微調，以及矩形邊緣自動裁切。
- **Focus Stack（全景深合成）** 可合成景深包圍序列，具備視差校正與接縫感知融合。

### 🖼️ RAW 以外的編輯

- **JPEG、WebP、PNG、TIFF 與 BMP 可在 Adjust 面板開啟**（<kbd>E</kbd>），並完整支援非破壞性 XMP 側邊欄檔——曝光、對比、相對白平衡、曲線、HSL、邊角消退、降噪、Dodge & Burn、Spot Heal 與幾何變形，皆不更動原始檔。
- **超過 8 bit 的檔案以實際位元深度編輯**——16-bit TIFF 在編輯器中保留全部 65,536 階，而非其中的 256 階。這對本 App 自己的輸出影響最大——HDR 合成、全景與景深合成都輸出 16-bit TIFF，提亮暗部時可維持平順，不會出現階調斷層。
- **Nikon HE/HE\* NEF 可透過內嵌 JPEG 編輯。** LibRaw 仍無法解馬賽克，因此基底為 8 bit：白平衡與亮部復原的餘裕較少，其餘行為與編輯 JPEG 相同。無法解馬賽克的 RAW 會繼續顯示內嵌預覽，而不是灰色方塊。

### 🎨 色彩與鏡頭

- **以色卡進行相機色彩校正**，具備互動式對話框、角點控制點與自動偵測。抽樣 24 色塊以推算 RGB 曲線、白平衡偏移與 HSL 差值，並綁定相機 EXIF 的 `Make`／`Model`，日後該機身拍攝的相片會自動套用。
- **校正檔會記錄當時所依據的解碼版本**，若解碼改變會提出說明——校正是建立在特定解碼結果之上的修正，LibRaw 更新可能會讓其基準改變。
- **變形鏡頭反壓平**（1.33×、1.5×、1.6×、2.0×），預覽與匯出皆即時套用。局部遮罩、Dodge & Burn 與 Heal 會對齊反壓平後的畫面——含預覽、settle 與 Compare。
- **手動鏡頭變形校正**，適用於沒有描述檔的鏡頭——正值校正枕狀變形，負值校正桶狀變形。

### 📤 匯出與 AI

- **匯出格式依來源自動調整。** RAW 提供 16-bit TIFF、JPEG 與 WebP；一般影像檔提供 JPEG 與 WebP。
- **AI Upscale 2×（Real-ESRGAN）** 與 **AI Denoise（SCUNet）** 為 Plus 專屬匯出變體，且僅在模型確實存在於本機時顯示。Standard 即使本機碰巧有模型檔也不會提供這些選項。
- **AI 降噪更快**——CPU 平台改用 NAFNet，並新增半解析度模式搭配細節還原——同時新增 **AI Detail** 滑桿控制降噪與細節的平衡。
- **天空遮罩模型僅 84 MB**，為半精度匯出版本，輸出與完整精度版本完全相同。
- **修正匯出接縫**，以及分段匯出路徑上 AI 降噪被靜默略過的問題。
- **抖動改於編碼空間進行**（而非線性空間），修正暗部雜訊。

### ⚡ 效能

- **大尺寸檔案上塗抹遮罩依然流暢**——在 2200×3300 基底上每次筆畫更新約 32 ms。筆刷每次落點不會重繪整張相片，改為更新疊色；放開筆刷時才完整算圖一次。
- **開啟已編輯相片明顯更快**——側邊欄檔只解析一次並共用，遮罩覆蓋範圍以半解析度儲存。
- **圖庫優先載入你正在看的位置。** 佇列中的工作會依目前可視範圍重新排序，而非依請求順序，因此跳轉後不必等待先前捲過的內容。
- **切換資料夾不再清空縮圖快取。** 先前的快取清理錯誤會在每次切換時清除所有縮圖——3000 個檔案的資料夾需時 27 秒。
- **消失的圖庫縮圖會恢復。** 先前一次暫時性失敗會讓灰色佔位圖被永久快取至該次工作階段結束。
- **觸控板捲動更順暢**，不再捨棄次像素位移量。
- **進入編輯器不再強制重新解碼。** 先前只是開啟再關閉、未做任何更動，也會清除該相片的所有快取層級。
- **完整影像快取改以記憶體用量為上限**，而非固定的 8 張，因此較小的檔案可保留更多張。

### 🖥️ 面板、圖庫與快捷鍵

- **鍵盤快捷鍵速查表**，位於面板的「i」按鈕，依用途分組，並會依你正在瀏覽、編輯或比對而變化。
- **本 App 產生的檔案會立即出現在圖庫**——HDR 合成、全景或匯出後不需重新開啟資料夾。
- **已編輯相片的圖庫縮圖會更新**，不再永遠顯示內嵌 JPEG。
- **Apply to Burst Group** 可將色調、色彩與降噪設定套用至整組連拍，同時保留每張相片各自的裁切與局部遮罩。
- **<kbd>↓</kbd> 僅在單張檢視中丟棄相片**，編輯中不會觸發；Looks（LUT／XMP）可直接重新命名。
- **各區段預設收合**（Histogram 與 Light 除外），並在該次工作階段記住你的排列方式。
- **遮罩分頁隱藏 Copy／Paste**——它們攜帶的是全域編輯、從不包含遮罩，放在該頁會讓人以為可以複製遮罩，實際上並不會。
- **安裝程式清楚呈現各版本能力**（Browse + Basic Adjustment、AI search + denoise、GPU RAW processing）。
- **更新提示會顯示發布說明。**

### 🔧 底層修正

本週期修正了十一輪程式碼審查發現的問題，包括在工作執行緒上建立 `QPixmap`、多個取消路徑可觸發的解碼配額重複釋放、以 `id()` 為鍵而可能在位址重用後比對到不同物件的登錄表、以每實例鎖保護共用狀態，以及在錯誤執行緒上釋放 CUDA-GL 資源。

### 升級說明

覆蓋安裝 3.0.2 即可，本次更新不需要清除快取。

---

## 🚀 版本 3.0.2
**發布日期：2026 年 7 月 22 日**

### 使用者可見變更

- **AI 降噪回來了——而且更快。** SCUNet 真實影像降噪改以 **ONNX Runtime** 執行，不再需要導致它在 3.0.1 被移除的數 GB PyTorch 安裝。在 DirectML 上以 **fp16** 執行——每個圖塊比 fp32 版本快約 **25 倍**，畫質沒有可察覺的差異。
- **AI 降噪現在是明確的匯出選項，不再自動套用。** 匯出選單新增 **AI Denoise（Restormer/SCUNet）** 子選單，內含專屬的 16-bit TIFF／JPEG／WebP 項目——點選其一即以 AI 降噪匯出；下方原本的格式項目仍使用 Noise Reduction 中設定的 Chroma NR 方法，不受影響。現在一次點擊就能同時決定格式與是否使用 AI 降噪，不再是容易忘記勾選的獨立核取方塊。
- **AI 降噪匯出現在可以取消，並顯示真實進度。** 先前在 AI 降噪執行期間按取消完全沒有反應——該處理過程無法被中斷，且整段（往往長達數分鐘）都不回報進度，看起來像當機。匯出現在會顯示即時的「Denoising (AI)…」進度，並會無限回應取消。
- **僅限 Plus，隨其他 AI 模型下載**（約 100 MB，併入原本約 600 MB 的 AI 模型下載——現在總計約 700 MB）。Standard 維持既有的色度／亮度雜訊抑制，無變更。

### ✨ 更流暢、更迅速的編輯體驗

- **滑桿調整即時反應：** 拖曳 Exposure（曝光）、Temperature（色溫）、Denoise（降噪）、Vignette（暗角）及 Dodge/Burn 滑桿時更加順暢。全管線已全面啟用多核心平行處理，即使在經過深度修圖的相片上也能保持零延遲流暢反應。
- **開啟相片速度倍增：** 開開啟帶有既有編輯紀錄的相片速度提升約一倍，並會顯示「Loading previous edit…」提示，避免重覆計算預覽畫面。

### 🖌️ 更聰明、更自然塗抹的 Dodge & Burn 筆觸

- **防斑點流暢筆觸：** 在相片上塗抹時可呈現勻稱自然的修飾效果。塗抹過程中手部停頓或移動較慢時，不再產生意外的過暗或過亮斑點。
- **筆觸狀態更穩定：** 切換 Dodge、Burn 與 Eraser 工具後，滑鼠游標在 Adjust 面板與相片區域間移動時不會再意外失效，全程保持武裝狀態。
- **遮罩覆疊流暢不卡頓：** 開啟遮罩覆疊視圖（<kbd>M</kbd>）時，遮罩繪製會即時跟隨筆刷軌跡，不再每次取樣都重繪整張畫面。
- **更精準的 Edge Assist 邊緣識別：** 邊緣輔助會自動適應相片雜訊，精準沿著物件輪廓停止，並能靈敏識別亮度相同但顏色不同的色彩邊界。

### 🎯 操控與介面優化

- **修圖時不再意外捲動或放大：** 開啟 Adjust 面板編輯時，影像區域停用純滑鼠滾輪，徹底解決捲動時意外將畫面放大至 35% 且無法縮回或跳至別張相片的問題。（按住 `Ctrl` + 滾輪縮放與滾輪調整筆刷大小功能不受影響）。
- **隨時按 <kbd>M</kbd> 鍵檢視遮罩：** **Mask (M)** 按鈕現已標示快捷鍵 <kbd>M</kbd>，只要相片包含修飾紀錄即可隨時開關檢視，無需先啟用筆刷。
- **即時更新遮罩清除：** 清除 dodge/burn 或 heal 筆觸時，相片畫面會立即更新呈現最新結果。
- **工作管理員顯示正確名稱：** RAWviewer 行程在工作管理員／活動監視器中現在會明確顯示為「RAWviewer」，不再顯示為泛用的「Python」。

### 升級

覆蓋安裝 3.0.1 即可，本次更新不需要清除快取。

---

## 🚀 版本 3.0.1
**發行日期：2026 年 7 月 20 日**

### 使用者可見變更

- **更小的 Windows Plus（CUDA）** — GPU demosaic 改用 **CuPy**，不再安裝數 GB 的 PyTorch。體積約 **1.5–2 GB**（另加 MobileCLIP 模型），與 Plus（DirectML）同級。
- **版本名稱更清楚** — 安裝精靈與介面為 **Standard**、**Plus（DirectML）**、**Plus（CUDA）**。
- **啟動更快** — 更快進入首屏（AI 搜尋相關模組改為開啟搜尋時才載入）。
- **匯出變更** — 已移除匯出時的 **SCUNet AI 降噪**。請改用 Adjust 色度降噪，再匯出 JPEG／WebP／TIFF。

### 修正與改進

- **穩定的圖庫捲動與載入：**
  - 修正快速捲動圖庫時可能發生的介面凍結與雙指觸控板捲動卡頓問題。
  - 修正高解析度縮放（如 Windows 125%/150% 縮放）螢幕下，跨區域跳轉圖庫時可能出現縮圖空白或載入卡死的問題。
  - 最佳化資料夾掃描與編輯狀態偵測，避免在外部硬碟或慢速隨身碟上快速瀏覽相片時產生卡頓。
  - 修正部分電腦上可能發生的圖庫縮圖載入停滯或卡死問題。
- **穩定性提升：**
  - 修正重建快取時可能發生的系統死鎖 (Deadlock) 問題。
  - 修正批次刪除或移動相片後，圖庫畫面會突然跳回最頂端的問題；現在畫面會維持在鄰近的其他相片上。
  - 修正 macOS 26+ Beta 測試版中語意搜尋可能發生的當機問題。
  - 提升 macOS Plus 版本的功能完整性，包含修正 Dock 圖示顯示、圖庫一致性與人臉掃描功能。
- **易用性與疑難排解：**
  - 開啟圖庫相片時優先載入內嵌預覽圖，實現即時開啟與顯示。

### 升級

覆蓋安裝 3.0.0 即可。建議清一次快取（Setup 勾選或 `clear_cache.bat`／**Clear Cache.command**）。

---

## 🚀 版本 3.0.0
**發行日期：2026 年 7 月 18 日**

RAWviewer 3.0 讓檢視器蛻變為完整的**篩片＋顯影**工具：完整整合的非破壞式 **Adjust／Develop 編輯器**、**星級評分**，以及全面重建、**明顯快過 2.5** 的影像載入管線——冷啟動實測圖庫就緒約快 **3×**，RAW 全解析度方向鍵導航中位數約快 **1.6–1.7×**（Lite 與 Full 皆然）。

### ✨ 新功能

#### 🎨 Adjust／Develop 編輯器（E）
> [!WARNING]
> **聲明：** 編輯功能目前為實驗性。無法保證相容所有相機型號，特別是新發售的機身。
- 非破壞顯影寫入 **XMP sidecar**，RAW 檔永遠保持原樣。瀏覽介面預設顯示原始像素（設 `RAWVIEWER_SIDECAR_ADJUST=1` 可在瀏覽時套用已存編輯）。
- **色調與色彩：** 曝光／對比／高光／陰影／白色／黑色，點式＋參數式**色調曲線**（RGB 與各通道）、**HSL 色彩混合器**、飽和度／自然飽和度。
- **白平衡：** As Shot（EXIF 有 Kelvin 時）、預設值（日光／陰天／陰影／鎢絲燈／螢光燈／閃光燈）、滴管，以及**自動白平衡**估算。
- **變形：** 互動式**裁切覆疊**與比例預設（自由／原始／1:1／4:3／3:2／16:9）、**拉直（含 Auto）**、梯形透視校正、可選鏡頭校正。
- **局部工具：** **Dodge & Burn**（柔邊圓形筆刷、Edge Assist、遮罩覆疊）；**Heal（H）** 修復筆刷，用於除塵除污。Lite 與 Full 皆可用。
- **效果與細節：** 暈影（Amount／Midpoint）、去霧、銳利度、可選方式的色度降噪。
- **風格：** **Creative LUT**（`.cube` 拖放＋管理庫）與受管理的 **XMP 預設**——匯入、套用、移除。
- **匯出** JPEG／WebP／16 位元 TIFF，含進度對話框與取消。
- 單張檢視工具列新增 **Adjust 按鈕**，可開關編輯器（與 **E** 快捷鍵相同，開啟時亮起）。

#### ⭐ 星級評分
- 數字鍵 **1–5** 評分（**0** 清除），或點擊單張檢視的星星。**↑ 不再切換書籤**（圖庫 ↑ 改為捲動；比較模式 ↑ 仍為提升候選）。圖庫請用底部**星級篩選**縮小範圍。
- 星級寫入 **XMP** 並顯示於圖庫縮圖徽章；圖庫可篩選**星級 ≥ N**，可與書籤篩選並用。

#### ⚡ 比 2.5 更快（冷啟動實測）
**相對 2.5 Lite 的重點結果（同一台 Windows、每次測試前清快取）：** 圖庫就緒 **8.6 秒 → 約 3 秒（約 2.7–2.9× 更快）**；RAW 全解析度導航中位數 **0.95 秒 → 約 0.6 秒（約 1.6–1.7× 更快）**。**Lite 與 Full 共用同一套 3.0 載入管線**，在篩片關鍵路徑上都明顯勝過 2.5；Full 可選 GPU demosaic（Windows CUDA 建置為 CuPy），在這些測試中速度與 Lite 同級。

- **Fast RAW 解碼**預設開啟：半尺寸與全感光元件層共用同一次 LibRaw unpack，色彩一致性經驗證（黃金 ARW/CR3 測試集 ±1 個 8-bit LSB）。
- 冷啟動圖庫縮圖預熱約快 **3×**；Canon CR3 內嵌預覽不再讀取整個檔案。
- 大型可選 ML 匯入延後至首次繪製之後；工作階段還原錯開全解析度解碼與預載，重啟即刻可用。
- **GPU demosaic 排程（完整版）：** 為畫面上的 CURRENT 圖保留最後一個 heavy 解碼槽、在 CURRENT 等待時搶佔鄰居 full、取消任務時立刻釋放槽位——修正鄰居預載佔住 `raw_limit=1` 導致當前檔永遠排不上 `stages=['full']` 的卡住。
- **LibRaw 不支援的 RAW（例如 Nikon HE/HE*）：** 僅對該檔使用內嵌預覽——不再把全應用工作流程切成 embedded JPEG（先前會讓資料夾後續所有檔都關掉 GPU demosaic）。
- RAW+GPU 下，當前 full 已在飛行中時會預熱 ±1 鄰居的感光元件 `full`（槽位保留仍優先 CURRENT）。CUDA demosaic 並發維持自適應預設（RTX 級通常為 **2**）；再提高並未讓混合格式套件更快。

##### 冷啟動套件對比 2.5（Windows，2026 年 7 月）
同一機器，**每次**測試前清除 `~/.rawviewer_cache`。圖庫：在約 6900 張的資料夾開啟一張 RAW，等 EXIF 排序後按住 **Up** 20 秒。RAW 導航：在約 260 張混合格式 RAW 資料夾開啟單檔，等感光元件級 full-res 繪製後再下一張（完整一圈）。

| 相對 2.5 基準 | 圖庫就緒 | 相對 2.5 | RAW 導航中位數 | 相對 2.5 |
|-------------|----------|----------|----------------|----------|
| **2.5 Lite** | 8.6 秒 | — | 0.95 秒 | — |
| **3.0 Lite 與 Full** | **約 3 秒** | **約 2.7–2.9× 更快** | **約 0.6 秒** | **約 1.6–1.7× 更快** |

Lite（CPU Fast RAW）與 Full（可解碼檔的 GPU demosaic）在冷啟動套件上的跑次差異僅數個百分點——兩者相對 2.5 皆屬同一量級。

| 相對 2.5 基準 | RAW 導航平均 | RAW 導航 p95 | 20s Up 縮圖數 |
|-------------|--------------|--------------|---------------|
| **2.5 Lite** | 1.20 秒 | 2.70 秒 | 1043 |
| **3.0 Lite 與 Full** | **約 0.7–0.8 秒** | **約 1.8–2.6 秒** | **約 940** |

**總結：** 從 2.5 升級到 3.0，在「開資料夾 → 進圖庫」與 RAW 高品質瀏覽上都有清楚的速度優勢。選 **Standard** 可得到最小安裝；選 **Plus** 可獲 AI 搜尋（Windows CUDA 另有 CuPy GPU demosaic）——在這些路徑上篩片速度同級。

#### 🖼️ 圖庫載入與捲動大改
- 圖庫填入時的主執行緒卡頓已消除（最嚴重卡頓 **11.7 秒 → 少於 0.6 秒**）：分批次填入縮圖、延後快取命中傳遞、外接磁碟不再逐格讀 sidecar。
- **錨點優先進入：** 從資料夾中段的相片開啟圖庫時，先載入該相片周圍的列。
- 滑鼠滾輪與觸控板捲動的速度與按住方向鍵一致，增益可調。
- 縮圖徽章（★ 星級／連拍 ×N／已編輯 ✎）與縮圖同時顯示。

#### 📷 格式
- **Nikon 高效率（HE／HE\*）NEF** 現可經內嵌 JPEG 瀏覽、篩片與評分——不再誤報「不支援或損毀」。在解碼器問世前，HE 檔案的 RAW 顯影維持停用。

#### 📦 更精簡的 Lite 版
- **Lite 保留完整 Adjust 編輯器**（CPU Fast RAW），但不含 PyTorch／kornia——安裝維持約 500 MB 等級。
- Full（CUDA）在後端可用時仍優先使用 GPU demosaic。

#### 🎨 設計
- 全新**暗房風格 App 圖示**（圖庫格＋色調曲線格）與高解析度啟動畫面資產。
- 所有元件與介面共用暗房色票。
- macOS 正式版內建 **OpenMP 版 LibRaw**，解碼可平行化。

### 🔧 自 2.5 以來的修正

- **方向：** 冷開啟／首次繪製的方向錯誤（橫躺、顛倒或首張 RAW 空白）已修正。
- **導覽競態：** 快速按方向鍵不再卡在過期影像或被取消的解碼上。
- **圖庫重建捲動：** 重建／中繼資料刷新時以進入點／當前圖為捲動錨點，不再跳到左上角。
- **GPU full 解碼飢餓：** 取消的 heavy RAW 任務會立刻釋放並發槽；快取命中不再把 ≥1024px 預覽當成已完成的感光元件解碼。
- **圖庫點擊縮放：** 點擊圖庫縮圖一律以「符合視窗」開啟，不再繼承前一視圖的縮放。
- **EXIF 快取競爭：** 讀取不再全部擠過同一把全域鎖（大型資料夾數秒導覽卡頓已修正）。
- **RAW 復原預覽（P）：** rawpy 邊界情況已修正。
- **篩片與介面細節：** 篩片縮放異常、Windows 啟動時工作列閃爍、丟棄相片無法返回、圖庫多選緩慢皆已修正。

### ⚠️ 重大變更：移除 macOS EDR

2.5 的 macOS EDR（HDR 顯示）路徑在 3.0 **移除**——它與新的快速載入管線不相容，會讓 RAW 解碼變得極慢。HDR 靜態影像（HEIC／HEIF／AVIF／HDR TIFF）改以 SDR 色調映射顯示。在不犧牲速度的前提下恢復 EDR 見 README 的[展望](README.zh-TW.md#展望)。

### 環境變數（新增／重要）

| 變數 | 預設 | 效果 |
|------|------|------|
| `RAWVIEWER_FAST_RAW_DECODE` | `1` | Fast RAW 路徑；`0` 回退至 rawpy |
| `RAWVIEWER_USE_PROCESS_POOL` | 自動 | 強制開／關 LibRaw process pool |
| `RAWVIEWER_SIDECAR_ADJUST` | `0` | 在瀏覽／全解析度像素套用已存 XMP 編輯（需啟用編輯）。預設關閉：瀏覽顯示原始、編輯在 Adjust 呈現 |
| `RAWVIEWER_UNPACK_STASH_SLOTS` | `3` | 保留幾份 LibRaw unpack 供半→全／A↔B 重訪（1–8） |

### 升級後建議測試

1. **既有使用者（建議一次）：** 安裝 v3 後清除快取再開啟資料夾。舊安裝在清除快取（或設 `RAWVIEWER_PERF_V2=1`）前仍使用舊版搜尋／索引效能模式。**Windows：** 安裝時勾選 **Clear existing cache**（或執行 `RAWviewer.exe` 旁的 `clear_cache.bat`）。**macOS：** 使用發行 zip 內的 **Clear Cache.command**／`clear_macos_cache.sh`。不會刪除相片或 XMP。
2. 開啟混合 ARW／CR3／NEF（含 HE\*）：方向鍵瀏覽、縮放至 100%、確認方向正確。
3. 以 **1–5** 評分、以星級篩選圖庫、確認 sidecar。
4. 開啟 Adjust（**E** 或滑桿按鈕）：試白平衡預設、裁切、Dodge/Burn、Heal、暈影／去霧、Creative LUT；儲存後確認 RAW 旁出現 XMP sidecar。

---

## 較早版本

2.5 及更早版本的發行說明請見英文版 [`RELEASE_NOTES.md`](RELEASE_NOTES.md)。
